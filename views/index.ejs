<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title %></title>
    </head>
    <body>
        <button>Generate Dummy Data</button>
        <script>
            document.querySelector("button").addEventListener("click", async (e) => {
                let response = await fetch("/generate").then((res) => {
                    return res.text();
                }).then((res) => {
                    console.log(res);
                });
            });
        </script>
    </body>
</html>
